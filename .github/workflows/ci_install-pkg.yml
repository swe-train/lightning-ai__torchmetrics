name: Install pkg

on:
  push:
    branches: [master, "release/*", "ci/try-reusable-workflows"]
  pull_request:
    branches: [master, "release/*"]

jobs:
  check:
    uses: PyTorchLightning/pytorch-lightning/.github/workflows/_check-package.yml@ci/define-reusable-workflows
    with:
      import-name: torchmetrics
      pypi-name: torchmetrics
