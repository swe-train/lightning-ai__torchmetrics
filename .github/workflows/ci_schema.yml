name: CI action schema

on:
  push:
    branches: [master, "release/*", "ci/try-reusable-workflows"]
  pull_request:
    branches: [master, "release/*"]

jobs:
  check:
    uses: PyTorchLightning/pytorch-lightning/.github/workflows/_check-schema.yml@ci/define-reusable-workflows
    with:
      azure-dir: '.azure'
